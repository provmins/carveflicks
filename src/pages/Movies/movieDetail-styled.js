import styled from 'styled-components';

const MovieDetailContainer = styled.div``;

const StillCutBox = styled.div`
  padding: 20px;
  margin-bottom: 2rem;
  p {
    font-weight: 500;
    margin-bottom: 0.9rem;
  }
`;

const TrailerBox = styled.div`
  padding: 20px;
  height: 18rem;
  margin-bottom: 2rem;
  p {
    margin-bottom: 10px;
  }
`;

export { MovieDetailContainer, StillCutBox, TrailerBox };
